%h1 Packings

= link_to 'New Packing', new_packing_path, class: "btn btn-default"
Total Packings:
= @count

%br
%br

%table.table.table-condensed.table-striped.table-hover.table-bordered
  %tr
    %th No.
    %th Type
    %th

  - @packings.each do |packing|
    %tr
      %td= packing.id
      %td= link_to packing.packing_type, edit_packing_path(packing)
      - if current_user.try(:admin)
        %td= link_to 'Destroy', packing, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br


