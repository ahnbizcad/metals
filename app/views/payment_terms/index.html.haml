%h1 Payment Terms

= link_to 'New Payment term', new_payment_term_path, class: "btn btn-default"

%table.table.table-condensed.table-striped.table-hover.table-bordered
  %tr
    %th No.
    %th
    %th

  - @payment_terms.each do |payment_term|
    %tr
      %td= payment_term.id
      %td= link_to payment_term.type, edit_payment_term_path(payment_term)
      - if current_user.try(:admin)
        %td= link_to 'Destroy', payment_term, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br


