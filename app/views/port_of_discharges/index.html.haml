%h1 Ports of Discharge

= link_to 'New Port of Discharge', new_port_of_discharge_path, class: "btn btn-default"
Total Ports of Discharge:
= @count

%br
%br

%table.table.table-condensed.table-striped.table-hover.table-bordered
  %tr
    %th No.
    %th City
    %th Country

  - @port_of_discharges.each do |port_of_discharge|
    %tr
      %td= port_of_discharge.id
      %td= link_to port_of_discharge.city, edit_port_of_discharge_path(port_of_discharge)
      %td= link_to port_of_discharge.country, edit_port_of_discharge_path(port_of_discharge)
      -# if current_user.try(:admin)
      %td= link_to 'Edit', edit_port_of_discharge_path(port_of_discharge)
      %td= link_to 'Destroy', port_of_discharge, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br


