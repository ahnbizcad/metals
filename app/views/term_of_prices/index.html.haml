%h1 Terms of Price

= link_to 'New Term of Price', new_term_of_price_path, class: "btn btn-default"
Total Terms of Price:
= @count

%br
%br

%table.table.table-condensed.table-striped.table-hover.table-bordered
  %tr
    %th No.
    %th Term
    %th Text

  - @term_of_prices.each do |term_of_price|
    %tr
      %td= term_of_price.id
      %td= term_of_price.term
      %td= term_of_price.text
      -# if current_user.try(:admin)
      %td= link_to 'Edit', edit_term_of_price_path(term_of_price)
      %td= link_to 'Destroy', term_of_price, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

